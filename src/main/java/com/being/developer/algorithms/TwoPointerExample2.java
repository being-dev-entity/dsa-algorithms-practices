package com.being.developer.algorithms;

/**
 * Two Sum: Find two numbers in an array that add up to a specific target sum.
 * Two pointers, one starting from the beginning and the other from the end,
 * move towards each other until they find the pair.
 */
public class TwoPointerExample2 {
   // refer TwoSum class in arrays package
}
